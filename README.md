# Scrapy-Streamlit-WebSite
In this project, data was extracted using both Scrapy and Beautiful Soup examples, and then saved to a PostgreSQL database. The data was visualized using Streamlit.
To run it, type "stream run OrderList.py" in the terminal. To extract data, type "scrapy crawl apartmentForRent -o data.json".
